import * as React from "react";
import Map from "./map";
import Menu from "./menu";

const Global = () => (
    <div className={"columns"}>
        <Map />
        <Menu />
    </div>
);

export default Global;
